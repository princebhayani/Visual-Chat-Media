import { Router } from "express";

const router = Router();

// Auth routes can be added here in the future if needed
// Currently, authentication is handled entirely by Firebase on the frontend

export default router;

